# 每日作业报告（Day 3）

## 1. 本日问题

- 里程碑：D3
- 学生或小组：哈哈哈组｜成员：刘泽润、吴升峰、程霖垚、韦佳瑜
- 使用者：希望提前看到用电变化、但不能依赖昂贵系统的家庭能源研究小组
- 真实输入：Kaggle Household Electric Power Consumption（2,075,259 条按分钟记录）
- 需要的输出：把真实分钟记录聚合成小时平均功率，比较持续性基线（最近一小时功率作为下一小时预测）与使用过去滞后特征的固定回归模型，预测下一小时平均功率
- 与使用者最相关的错误：高需求时段被漏报或误报，尤其是真实用电突增却被预测为低值
- 本日产品边界：只做一户家庭历史数据实验，不能代表其他家庭，也不能直接用于电网控制、费用控制或安全告警

## 2. 真实数据或真实课程输入

- 所有者/发布者：UCI Machine Learning Repository，由 Kaggle 用户 uciml 发布
- 标题：Household Electric Power Consumption
- 原始 URL：https://www.kaggle.com/datasets/uciml/electric-power-consumption-data-set
- 许可标签或使用许可：Kaggle 数据集页面标注（以页面为准）
- 下载/取得日期：2026-08-20
- 预期文件与结构：`data/raw/household_power_consumption.txt`，分号分隔，含 `Date`、`Time`、`Global_active_power` 等 9 列
- 检查命令：`python analyze.py --check-data`
- 实际检查结果：

```
REAL DATA CHECK PASSED
rows: 2075259
columns: 9
time_range: ["2006-12-16 17:24:00", "2010-11-26 21:02:00"]
```

- 建模窗口：课程 starter 默认读取前 150,000 条分钟记录，聚合后得到 2,501 个小时样本；按时间顺序 80/20 划分后训练 1,980 个、测试 496 个
- 已知缺失、偏差或限制：数据只有一户家庭；原始分钟记录中存在缺失值；建模窗口不是全量四年数据，不能推广到其他家庭或其他用电模式

## 3. 可复现运行

```powershell
# 当前目录
cd <本仓库根目录>

# 安装
python -m pip install -r requirements.txt

# 数据检查
python analyze.py --check-data
# 预期：REAL DATA CHECK PASSED；rows: 2075259

# 准备小时级数据（默认读取前 150000 条分钟记录）
python analyze.py --prepare

# 主程序：基线与候选在同一时间顺序划分上比较
python analyze.py

# 测试
python -m unittest discover -s tests -v
# 预期：Ran 2 tests ... OK
```

关键输出文件位置：`metrics.json`（指标）、`forecast.png`（留出测试周曲线）、`largest_errors.csv`（误差最大的 12 个样本）、`data/processed/hourly_power.csv`（聚合后的小时数据）。

## 4. 基线与候选

### 简单基线

- 方法：持续性基线，用最近一小时功率 `lag_1` 作为下一小时预测
- 为什么足够简单：零学习，只回答“如果下一小时和刚过去的一小时一样会怎样”，用来给候选模型提供最低比较标准
- 命令：`python analyze.py`
- 结果：MAE 0.7937 kW；RMSE 1.1118 kW；高需求召回 0.1842

### 候选方法

- 学生完成的核心改动：在 `analyze.py` 中完成 `make_lagged` 和 `build_candidate`
- 滞后特征：`lag_1`、`lag_2`、`lag_3`、`lag_24`，以及 `hour_of_day`
- 模型：`RandomForestRegressor(n_estimators=100, random_state=2026, n_jobs=-1)`，固定随机种子
- 保持不变的数据、划分、指标或参数：真实数据、时间顺序划分、测试区间、MAE/RMSE 指标均未改动
- 命令：`python analyze.py`
- 结果：MAE 0.5779 kW；RMSE 0.7919 kW；高需求召回 0.1053

| 项目 | 基线 | 候选 | 含义 |
| --- | ---: | ---: | --- |
| MAE (kW) | 0.7937 | 0.5779 | 每个小时预测值与真实值的平均绝对偏差 |
| RMSE (kW) | 1.1118 | 0.7919 | 对大误差惩罚更重的平均误差 |
| 高需求召回 | 0.1842 | 0.1053 | 真实高需求小时被识别出来的比例 |
| 高需求误报 | 31 | 12 | 低需求小时被错误标记为高需求 |
| 高需求漏报 | 31 | 34 | 真实高需求小时没有被标记 |

**结论**：同一数据、同一时间顺序划分、同一指标下，随机森林候选的平均误差明显低于持续性基线（MAE 从 0.7937 降到 0.5779），误报也从 31 降到 12。但候选的高需求召回更低（0.1053），漏报 34 次比基线多 3 次，所以不能只看平均误差；对家庭能源研究小组来说，高需求时段漏报仍是需要重点检查的风险。

## 5. 一个真实失败案例

- 样本位置/编号：`largest_errors.csv` 中的 `2007-03-19 19:00:00`
- 真实结果：4.1021 kW（高于高需求阈值 3.1617 kW）
- 系统输出：1.8359 kW（低于阈值，漏报高需求）
- 可以观察到什么：该小时位于傍晚用电高峰，真实功率明显高于阈值，但候选模型把下一小时预测成接近普通水平
- 说明的限制：模型对突发性高需求变化的反应不够灵敏，滞后特征只能反映过去一小时左右的状态，无法提前捕捉突变
- 不能证明什么：不能证明该小时必然漏报，也不能证明模型对所有傍晚高峰都不敏感；单个案例只能说明这类突增是当前特征下的困难样本
- 下一项最小检查：统计所有高需求漏报样本的时间分布，尝试加入滚动均值/滚动标准差或星期特征，检查能否提高高需求召回而不显著增加误报

## 6. 智能体与学生工作边界

智能体在本日任务中主要协助：

- 读取课程 starter、测试和队友 day1/day2 仓库结构，制定代码实现方案
- 完成 `make_lagged` 与 `build_candidate` 两个 TODO，并补充课程 README 要求的 `--check-data`
- 使用真实数据运行检查、准备小时数据、运行主程序和单元测试
- 起草 README、`report.md` 和 `submission.json` 框架

学生提交前必须完成：

- 打开 `analyze.py`，逐行解释滞后特征、时间顺序划分、基线和随机森林的实现
- 运行 `python analyze.py --check-data`、`python analyze.py` 和测试，核对 `metrics.json`、`largest_errors.csv` 中的数字与报告一致
- 检查 `git diff`，确认没有随机打乱时间顺序、没有目标泄漏、没有为了好看结果改动数据或测试
- 改写本报告中自己无法独立解释的句子

## 7. 结论与限制

本项目基于真实家庭用电分钟记录，把数据聚合成小时平均功率，按时间顺序划分训练集和测试集，比较了持续性基线与固定随机种子的随机森林回归模型。结果显示，候选模型在 MAE（0.5779 kW）和 RMSE（0.7919 kW）上都优于持续性基线（0.7937 kW / 1.1118 kW），说明滞后特征和随机森林能够利用历史模式降低平均预测误差。

但实验也显示，候选模型在高需求时段召回率更低（0.1053），且高需求漏报 34 次，说明平均误差改善不等于高需求时段预测改善。该结论只基于一户家庭、前 150,000 条分钟记录组成的数据窗口，不能推广到其他家庭，也不能直接用于电网控制、费用控制或安全告警。

因此本项目得到的最小可靠结论是：在该户真实数据窗口内，使用滞后特征和固定随机种子的随机森林可以降低下一小时平均功率预测的平均误差，但高需求时段漏报仍需要进一步优化和人工判断。

## 8. 提交复核

- [ ] README 从新环境可以开始运行
- [ ] 数据检查、测试和主程序重新运行
- [ ] 报告数字与 `metrics.json`、`largest_errors.csv` 保存输出一致
- [ ] `presentation.pptx` 在 3 分钟内讲完
- [ ] `submission.json` 路径正确
- [ ] 无密钥、大数据、私人信息、虚拟环境或缓存
- [ ] GitHub 网页复查并邮件发送 URL
